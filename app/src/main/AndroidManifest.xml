<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.demos.demos" >
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/AppTheme" >
        <activity android:name=".sort.SortActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".fragmenttest.FragmentTestActivity"/>
        <activity android:name=".saveinstanceTest.SaveTestActivity"/>
        <service android:name=".servicetest.Test2Service"/>
        <service android:name=".servicetest.TestService"/>
        <activity android:name=".servicetest.ServiceTestActivity"/>
        <activity android:name=".jni.JNIActivity"/>

        <activity android:name=".binder.BinderActivity" />
        <activity android:name=".handler.HandlerActivity" />
        <activity android:name=".okhttp.OkHttpActivity" />
        <activity android:name=".listview.RecyclerviewActivity" />
        <activity android:name=".listview.ListViewActivity" />
        <activity android:name=".listview.ViewpagerActivity" />
        <activity android:name=".rxjava.RxjavaActivity" />
        <!--<activity android:name=".sort.SortActivity" />-->

        <activity android:name=".MainActivity"/>
        <service android:name=".binder.PushService"
            android:process=":push"/>

    </application>

</manifest>