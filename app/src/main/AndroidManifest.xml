<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.test.demos.demos" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".MyApplication"
        android:theme="@style/AppTheme" >
        <activity android:name=".handler.HandlerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fragmenttest.FragmentTestActivity"/>
        <activity android:name=".saveTest.SaveTestActivity"/>
        <service android:name=".servicetest.Test2Service"/>
        <service android:name=".servicetest.TestService"/>
        <activity android:name=".servicetest.ServiceTestActivity"/>

        <activity android:name=".binder.BinderActivity"
            android:process=":binder"/>
        <activity android:name=".binder.Binder2Activity"
            android:process=":binder"/>
        <activity android:name=".MainActivity"/>

    </application>

</manifest>